# Legacy NSIS Installer

FileLocker now packages public releases with Inno Setup. The NSIS script in this folder is retained only as historical reference for older installer behavior and is not used by the active build, package, or updater flow.
